env = "development"

# EXPRESS
host = "0.0.0.0"
port = "8081"

# DICCIONARIO, CREA UNA TABLA LLAMADA DB
[db]
username = "root"
user = "root"
password = "Ortega_2020"
host = "127.0.0.1"
database = "prueba_schema"

# migration, seeder config
dialect = "mysql"
seederStorage = "sequelize"

# models default config
operatorsAliases = false

  [db.define]
  freezeTableName = true
  engine = "InnoDB"
  charset = "utf8mb4"
  collate = "utf8mb4_general_ci"

[session]
secret = 'XX update-for-prod-in_local-production.toml'
resave = true
saveUninitialized = true

  [session.cookie]
  maxAge = 6048e5 # 7 days
